# Smithery configuration file for hosted deployment
runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  # Configuration is passed via environment variables (see Dockerfile for defaults)
  # Available env vars: DOCS_PATH, SERVER_NAME, LOG_LEVEL, VERSION,
  #                     UPDATE_DOCS, FORCE_UPDATE, EXTERNAL_ONLY, CORE_ONLY